@model IEnumerable<DatabaseAccess.tblAccountSubControl>

@{
    ViewBag.Title = "Account Sub Controls";
}

<script src="~/Scripts/jquery-3.6.3.js"></script>

<div class="card">
    <div class="card-flat">
        <div class="card-header">
            @Html.ActionLink("New Account Sub Control", "Create", null, new { @style = "float: left;", @class = "btn btn-success" })
            <input class="form-control col-md-3" type="text" style="float: right;" id="searchaccountsubcontrol" name="searchaccountsubcontrol" placeholder="Search..." />
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover" id="dataTable">
                    <thead>
                        <tr>
                            <th>
                                Head Account
                            </th>
                            <th>
                                Control Account
                            </th>
                            <th>
                                Sub Control Account
                            </th>
                            <th>
                                User
                            </th>
                            <th>
                                Action's
                            </th>
                        </tr>
                    </thead>
                    <tbody id="accountsubcontrolslist">
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.tblAccountHead.AccountHeadName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.tblAccountControl.AccountControlName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.AccountSubControlName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.tblUser.FullName)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.AccountSubControlID }, new { @class = "btn btn-primary" })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        $("#searchaccountsubcontrol").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#accountsubcontrolslist tr").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });
</script>